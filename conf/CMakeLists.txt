CMAKE_MINIMUM_REQUIRED(VERSION 3.10)

FILE(GLOB conf_files "*.json")
INSTALL(FILES 
    ${conf_files}
    PERMISSIONS
    OWNER_READ
    GROUP_READ
    WORLD_READ
    DESTINATION ${SF_INSTALL_CONF_DIR}
)

FILE(COPY ${conf_files} DESTINATION ${SF_OUTPUT_CONF_DIR})