CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

FILE(GLOB conf_files "*.json")
INSTALL(FILES 
    ${conf_files}
    PERMISSIONS
    OWNER_READ
    GROUP_READ
    WORLD_READ
    DESTINATION ${ITOOLS_INSTALL_CONF_DIR}
)

FILE(COPY ${conf_files} DESTINATION ${ITOOLS_OUTPUT_CONF_DIR})